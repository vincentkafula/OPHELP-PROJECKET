{
  "name": "ophelp-platform",
  "private": true,
  "version": "1.0.0",
  "description": "OPHELP — Straatwerk voucher & operations platform. Monorepo: React/Vite frontend + Node/Express + PostgreSQL backend.",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "build": "npm run install:all && npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "migrate": "npm run migrate --prefix backend",
    "seed": "npm run seed --prefix backend"
  }
}
